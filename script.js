let students = [`radmir `, `shaxboz`, `jaxongir`, `xasan`, `daler`]

let even_names = []
let odd_names = []

students.map((d , c) => {
    if (ind % 2 === 0) {
        even_names.push(std)
    } else {
        odd_names.push(std)
    }
}

)

console.log(even_names);
console.log(odd_names);